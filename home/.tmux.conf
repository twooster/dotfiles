# Basic setup -- remap to C-a
set-option -g prefix C-a
bind-key C-a last-window

# Start at index 1, not 0, for key continuity
set -g base-index 1

# Easy splits
unbind %
bind | split-window -h
bind - split-window -v

